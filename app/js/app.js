import React from 'react';
import ReactDOM, { render } from 'react-dom';
import routes from './routes';
ReactDOM.render((routes), document.getElementById('reactComponent'));

/*
let greet = (message) => {
  console.log(`${message}`);
}

greet('hello world');
*/
